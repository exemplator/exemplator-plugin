'use babel';

import ExemplatorPluginView from '../lib/exemplator-plugin-view';

describe('ExemplatorPluginView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
